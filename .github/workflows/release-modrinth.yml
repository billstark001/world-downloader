name: Publish to Modrinth

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. 0.4.1)'
        required: true
        type: string
      release_channel:
        description: 'Release channel'
        required: true
        default: 'release'
        type: choice
        options:
          - release
          - beta
          - alpha
      changelog:
        description: 'Changelog text (leave empty to use CHANGELOG.md)'
        required: false
        type: string

jobs:
  publish-modrinth:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'microsoft'

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build
        run: ./gradlew buildAll

      - name: Collect distributable JARs
        shell: bash
        run: |
          rm -rf build/release
          mkdir -p build/release
          find versions -path '*/build/libs/world-mirror-*-fabric.jar' -type f \
            -exec cp '{}' build/release/ \;
          test "$(find build/release -maxdepth 1 -name '*.jar' -type f | wc -l)" -eq 3

      - name: Read CHANGELOG.md (fallback changelog)
        id: read_changelog
        if: ${{ inputs.changelog == '' }}
        run: |
          # Extract the first release section from CHANGELOG.md
          CHANGELOG=$(awk '/^## \[/{found++} found==1{print} found==2{exit}' CHANGELOG.md)
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Publish to Modrinth
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: ${{ vars.MODRINTH_PROJECT_ID }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          files: build/release/world-mirror-${{ inputs.version }}+*-fabric.jar
          name: World Mirror ${{ inputs.version }}
          version: ${{ inputs.version }}
          version-type: ${{ inputs.release_channel }}
          changelog: ${{ inputs.changelog || steps.read_changelog.outputs.changelog }}
          game-versions: |
            1.21.11
            26.1.2
            26.2
          loaders: |
            fabric
          dependencies: |
            fabric-api | required
